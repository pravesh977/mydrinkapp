<%= render "navmain/navmain" %>

<body class="editandnewform">
<h1>Hello, stranger! Care to share your drink with the world?</h1>
<%= form_for :drink, url: drinks_path, class: "editandnewform" do |f| %>
    <% if @drink.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@drink.errors.count, "error") %> prohibited
        this drink from being saved:
      </h2>
      <ul>
        <% @drink.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <p>
        <%= f.label :drink_Name %><br>
        <%= f.text_field :name %>
    </p>

    <p>
        <%= f.label :Drinkscription %><br>
        <%= f.text_area :description %>
    </p>
    <p>
      <%= f.label :Upload_image %>
      <%= f.file_field :drinkimage1 %>
    </p>
    <p>
        <%= f.submit "Share It" %>
    </p>
<% end %>
<%= link_to "Back to List", drinks_path %>
<%= link_to "Main Page", root_path %>
</body>